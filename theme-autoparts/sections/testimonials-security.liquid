<section class="container">
  <div class="testimonials">
    <div>
      <h2>{{ section.settings.heading }}</h2>
      <ul>
        {% for block in section.blocks %}
          <li class="card" {{ block.shopify_attributes }}>
            <div class="stars">{% for i in (1..block.settings.stars) %}★{% endfor %}</div>
            <p>{{ block.settings.text }}</p>
            <small>— {{ block.settings.author }}</small>
          </li>
        {% endfor %}
      </ul>
    </div>
    <aside>
      <h3>Sécurité & services</h3>
      <div class="badges">
        <span class="badge">🔒 Paiement sécurisé</span>
        <span class="badge">🚚 Livraison rapide</span>
        <span class="badge">✅ Garantie constructeur</span>
      </div>
    </aside>
  </div>
</section>

{% schema %}
{
  "name": "Avis & sécurité",
  "settings": [{"type":"text","id":"heading","label":"Titre","default":"Avis clients"}],
  "blocks": [
    {"type":"review","name":"Témoignage","settings":[
      {"type":"range","id":"stars","min":1,"max":5,"step":1,"label":"Étoiles","default":5},
      {"type":"text","id":"text","label":"Texte","default":"Excellent service et pièces de qualité."},
      {"type":"text","id":"author","label":"Auteur","default":"Jean M."}
    ]}
  ],
  "max_blocks": 6,
  "presets": [{"name":"Avis & sécurité","category":"Contenu"}]
}
{% endschema %}

